# Week 1 (Sep 8–14, 2026) - Getting set up

## What actually happened

I created the public GitHub repo `oim3690` and a personal site at [roycemy.github.io](https://roycemy.github.io). In Cursor, I asked AI to write a single-file `index.html` homepage: my name, a short paragraph, three interests, one image, and a GitHub link. I did not write that HTML from scratch. I opened the file, looked at it, and realized I could read some tags (`h1`, `p`, the list) but not the stuff at the top.

## What I learned

A webpage is just a file. The browser turns tags into a page. GitHub is where the files live so other people (and the course) can see them. I still do not fully understand git, but I know the repo has to be public and `index.html` has to sit at the root.

## Challenges

I am not an experienced coder. I was not sure which parts of the file I was allowed to change, or why some tags have extra words inside them like `lang="en"`. I also mixed up the course repo (`oim3690`) with my personal GitHub Pages repo (`roycemy.github.io`). They are two different places.

## AI usage

I used Cursor to generate `index.html`. I kept the page simple (plain HTML, no CSS framework) because that is what the prompt asked for. I did not want to submit a page I could not explain at all, so I started asking questions about the code instead of asking for more features.
